import 'package:flutter/material.dart';

class FichajeScreen extends StatelessWidget {
  const FichajeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Fichajes'),
      ),
      body: const Center(
        child: Text('Pantalla de Fichajes en construcción'),
      ),
    );
  }
}
